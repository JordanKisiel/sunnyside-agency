{
  "name": "sunnyside-agency",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tailwindcss -i ./input.css -o ./css/main.css && vite build",
    "watch": "tailwindcss -i ./input.css -o ./css/main.css --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8",
    "vite": "^3.1.0"
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
